From 45e5be1ff696534525e46741d284604b18c2f827 Mon Sep 17 00:00:00 2001
From: Xinyang Li <lixinyang411@gmail.com>
Date: Tue, 6 Jan 2026 18:46:10 +0200
Subject: [PATCH] local patch in my nixos-config

---
 lib/routes/caixin/utils-fulltext.ts |  31 +++++++---
 lib/routes/caixin/weekly.ts         | 107 ++++++++++++++++++++++++++---------
 2 files changed, 101 insertions(+), 37 deletions(-)

diff --git a/lib/routes/caixin/utils-fulltext.ts b/lib/routes/caixin/utils-fulltext.ts
index 3d1b01da0..e7112792d 100644
--- a/lib/routes/caixin/utils-fulltext.ts
+++ b/lib/routes/caixin/utils-fulltext.ts
@@ -10,13 +10,14 @@ import ofetch from '@/utils/ofetch';
 const rsaPrivateKey =
     '-----BEGIN PRIVATE KEY----------END PRIVATE KEY-----';
 
-export async function getFulltext(url: string) {
+function getAuthHeaders(url: string): { articleID: string; headers: Record<string, string> } | null {
     if (!config.caixin.cookie) {
-        return;
+        return null;
     }
     if (!/\d+\.html/.test(url)) {
-        return;
+        return null;
     }
+
     const articleID = url.match(/(\d+)\.html/)[1];
 
     const nonce = crypto.randomUUID().replaceAll('-', '').toUpperCase();
@@ -33,21 +34,33 @@ export async function getFulltext(url: string) {
     sig.updateString(rawString);
     const sigValueHex = hextob64(sig.sign());
 
+    return {
+        articleID,
+        headers: {
+            'X-Sign': encodeURIComponent(sigValueHex),
+            'X-Nonce': encodeURIComponent(nonce),
+            Cookie: config.caixin.cookie,
+        },
+    };
+}
+
+export async function getFulltext(url: string) {
+    const authData = getAuthHeaders(url);
+    if (!authData) {
+        return;
+    }
+
     const isWeekly = url.includes('weekly');
     const res = await ofetch('https://gateway.caixin.com/api/newauth/checkAuthByIdJsonp', {
         query: {
             type: 1,
             page: isWeekly ? 0 : 1,
             rand: Math.random(),
-            id: articleID,
-        },
-        headers: {
-            'X-Sign': encodeURIComponent(sigValueHex),
-            'X-Nonce': encodeURIComponent(nonce),
-            Cookie: config.caixin.cookie,
+            id: authData.articleID,
         },
+        headers: authData.headers,
     });
 
     const { content = '', pictureList } = JSON.parse(res.data.match(/resetContentInfo\((.*)\)/)[1]);
     return content + (pictureList ? pictureList.map((e) => `<img src="${e.url}" id="picture_${e.id}" alt="${e.desc}"><dl><dt>${e.desc}</dt></dl>`).join('') : '');
 }
diff --git a/lib/routes/caixin/weekly.ts b/lib/routes/caixin/weekly.ts
index c67ef5b04..28a59b68e 100644
--- a/lib/routes/caixin/weekly.ts
+++ b/lib/routes/caixin/weekly.ts
@@ -4,6 +4,7 @@ import type { DataItem, Route } from '@/types';
 import cache from '@/utils/cache';
 import got from '@/utils/got';
 import { parseDate } from '@/utils/parse-date';
+import { getFulltext } from './utils-fulltext';
 
 export const route: Route = {
     path: '/weekly',
@@ -14,6 +15,14 @@ export const route: Route = {
             source: ['weekly.caixin.com/', 'weekly.caixin.com/*'],
         },
     ],
+    features: {
+        requireConfig: false,
+        requirePuppeteer: false,
+        antiCrawler: false,
+        supportBT: false,
+        supportPodcast: false,
+        supportScihub: false,
+    },
     name: '财新周刊',
     maintainers: ['TonyRL'],
     handler,
@@ -22,11 +31,13 @@ export const route: Route = {
 
 async function handler(ctx) {
     const link = 'https://weekly.caixin.com';
+    const limit = ctx.req.query('limit') ? Number.parseInt(ctx.req.query('limit'), 10) : 35;
 
     const { data: response } = await got(link);
     const $ = load(response);
 
-    const list = [
+    // Extract weekly issue links
+    const weeklyIssues = [
         ...$('.mi')
             .toArray()
             .map((item) => ({
@@ -37,42 +48,82 @@ async function handler(ctx) {
             .map((item) => ({
                 link: $(item).attr('href'),
             })),
-    ].slice(0, ctx.req.query('limit') ? Number(ctx.req.query('limit')) : 10) as DataItem[];
+    ].slice(0, 2) as DataItem[]; // Limit to recent issues to avoid too many requests
 
-    const items = (await Promise.all(
-        list.map((item) =>
-            cache.tryGet(item.link, async () => {
-                const { data } = await got(item.link);
-                const $ = load(data);
+    const allArticles: DataItem[] = [];
 
-                item.title = $('head title')
-                    .text()
-                    .replace(/_财新周刊频道_财新网$/, '')
-                    .trim();
-                item.pubDate = parseDate(
-                    $('.source')
-                        .text()
-                        .match(/出版日期：(\d{4}-\d{2}-\d{2})/)[1]
-                );
+    // Process each weekly issue
+    for (const issue of weeklyIssues) {
+        if (!issue.link) continue;
 
-                $('.subscribe').remove();
+        const issueData = await cache.tryGet(issue.link, async () => {
+            const { data } = await got(issue.link);
+            const $ = load(data);
 
-                const report = $('.report');
-                report.find('.title, .source, .date').remove();
+            const articles = $('dl dt a')
+                .toArray()
+                .map((item) => {
+                    const $item = $(item);
+                    const articleLink = $item.attr('href');
+                    if (!articleLink || !articleLink.includes('weekly.caixin.com')) {
+                        return null;
+                    }
 
-                item.description = $('.cover').html() + report.html() + $('.magIntro2').html();
+                    // Make URL absolute if needed
+                    const fullLink = articleLink.startsWith('http') ? articleLink : `https://weekly.caixin.com${articleLink}`;
 
-                return item;
+                    return {
+                        title: $item.text().trim(),
+                        link: fullLink,
+                        guid: fullLink,
+                    };
+                })
+                .filter(Boolean) as DataItem[];
+
+            // Get issue publication date
+            const pubDateText = $('.source').text();
+            const pubDateMatch = pubDateText.match(/出版日期：(\d{4}-\d{2}-\d{2})/);
+            const issuePubDate = pubDateMatch ? parseDate(pubDateMatch[1]) : new Date();
+
+            return { articles, issuePubDate };
+        });
+
+        // Process articles from this issue
+        const articlesWithContent = await Promise.all(
+            issueData.articles.slice(0, limit - allArticles.length).map(async (article) => {
+                return await cache.tryGet(article.link, async () => {
+                    const { data } = await got(article.link);
+                    const $ = load(data);
+
+                    // Extract basic article info
+                    article.title = article.title || $('head title').text().replace(/_财新周刊频道_财新网$/, '').trim();
+                    article.pubDate = issueData.issuePubDate;
+
+                    // Extract article summary
+                    const summary = $('.summary').text() || $('dd').not('.date').first().text();
+
+                    // Try to get fulltext
+                    const fulltext = await getFulltext(article.link);
+                    article.description = fulltext || summary || '';
+                    article.itunes_item_image = $('meta[property="og:image"]').first().attr('content');
+
+                    return article;
+                });
             })
-        )
-    )) as DataItem[];
+        );
+
+        allArticles.push(...articlesWithContent);
+
+        // Stop if we've reached the limit
+        if (allArticles.length >= limit) {
+            break;
+        }
+    }
 
     return {
-        title: $('head title')
-            .text()
-            .replace(/_财新周刊频道_财新网$/, '')
-            .trim(),
+        title: '财新周刊',
         link,
-        item: items,
+        item: allArticles,
+        description: '财新周刊所有文章列表',
     };
 }
-- 
2.52.0
